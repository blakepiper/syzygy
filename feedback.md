- There is no way to delete a profile
- Tarot card art in the terminal does not display correctly
- I can't test the onboarding flow of setting up a local model or OpenAI /Claude API key because profile creation is bugged
- User should be able to set up llama.ccp by pressing 'm' for model from the main screen. Right now it just says "not reachable, no API key needed" so when you select it nothing happens
- Use our logo.svg in the app. 
- Everywhere that is currently piss yellow in the TUI should be switched to white
- For testing purposes, let's create a "reroll" function in the main display that allows the user to reroll today's card and recalculate the reading. So that I can test the animation over and over again and see new cards display, new readings
- There needs to be a function from the main menu to view today's "cosmos" horoscope, in a similar way that the user can press 'c' to view their natal chart.
- Both natal chart and the daily horoscope should have an LLM summary that the user can read.
- Change the font to Cinzel https://fonts.google.com/specimen/Cinzel?preview.text=Welcome%20to%20syzygy.%20Here%20is%20today%26apos;s%20reading&specimen.preview.text=Welcome+to+syzygy.+Here+is+today%26apos;s+reading&preview.script=Latn
- The astrological symbols that rotate around in the wheel animation are too small - make them larger. 
- 'r' to retry still seems to not work
- We need a startup animation, then a very cool welcome screen with "press any button to continue"
- We need an animation for after the user selects a self
- We need a transition animation for when a user selects to turn the wheel
- We need a transition animation for when a user opens today's reading
- Let's process the knowledge sources (the three books) and include them as committed artifacts in the repo that all users get (should be in machine readable, non-pdf form). Maybe json objects, vectorized something or other? Idk
- We now have a mascot.png that we need to incorporate
- We now have a theme song, called theme.mp3 that should play on a loop the whole time the application is running
- The current TUI does not make intelligent use of the space - the display feels largely empty. When thinking about the animations and an intelligent way to approach their design, factor that in. There is a new animation design spec saved at animation.md that you can read for some guidance