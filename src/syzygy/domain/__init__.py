"""Pure application domain models.

Nothing in `syzygy.domain` may import Textual, Kerykeion, or an LLM
provider SDK. These are the stable contracts the rest of the application
is built against (see AGENTS.md and IMPLEMENTATION_PLAN.md).
"""
